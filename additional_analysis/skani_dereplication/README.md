To run skani dereplication:

- Run `skani_derep.sh` with the genome list (RefSeq, NCBI, etc.)
- Run `get_reps.py` for a quick ANI98 and ANI99 cluster groups definition. No quality or genome size selection as we were only looking for cluster groups.
